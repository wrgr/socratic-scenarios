# Open-weight unlearning arm — Python deps (GPU strongly recommended for a real run).
# CPU install for the smoke test:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
#   pip install transformers peft accelerate safetensors
torch>=2.1
transformers>=4.48   # OLMo-2 (Olmo2ForCausalLM) needs >=4.47; force the upgrade even if Colab ships an older one
peft>=0.11
accelerate>=0.30
safetensors>=0.4
matplotlib>=3.7
